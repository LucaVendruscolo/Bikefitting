# Modal Backend Configuration
# Get these URLs from your Modal deployment output

# Server-side (private) - Streaming endpoint for processing
MODAL_STREAM_URL=https://YOUR-USERNAME--bikefitting-process-video-stream.modal.run

# Server-side (private) - API key for authentication
MODAL_API_KEY=your-secret-api-key-here

# Client-side (public) - Download endpoint for video playback
NEXT_PUBLIC_MODAL_DOWNLOAD_URL=https://YOUR-USERNAME--bikefitting-download.modal.run
