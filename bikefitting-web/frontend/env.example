# Modal Backend Configuration
# Get these URLs from your Modal deployment output

# Client-side (public) - Streaming endpoint for processing (called directly from browser)
NEXT_PUBLIC_MODAL_STREAM_URL=https://YOUR-USERNAME--bikefitting-process-video-stream.modal.run

# Client-side (public) - Download endpoint for video playback
NEXT_PUBLIC_MODAL_DOWNLOAD_URL=https://YOUR-USERNAME--bikefitting-download.modal.run
